module.exports = {
  bitkubChain: {
    megalandMarketPlaceContract: "",
  },
  defaultNFTABI: [
    "function tokenURI(uint256 tokenId) public view returns(string memory)",
    "function balanceOf(address owner) public view returns(uint256)",
  ],
};
